import 'package:flutter/material.dart';

class TrainingGuidePage extends StatelessWidget {
  const TrainingGuidePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("TrainingGuidePage")));
  }
}
