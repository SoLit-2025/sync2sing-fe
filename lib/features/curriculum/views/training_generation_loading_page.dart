import 'package:flutter/material.dart';

class TrainingGenerationLoadingPage extends StatelessWidget {
  const TrainingGenerationLoadingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("TrainingGenerationLoadingPage")));
  }
}
