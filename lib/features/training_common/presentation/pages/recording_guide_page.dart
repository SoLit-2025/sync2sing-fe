import 'package:flutter/material.dart';

class RecordingGuidePage extends StatelessWidget {
  const RecordingGuidePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("RecordingGuidePage")));
  }
}
