import 'package:flutter/material.dart';

class MaximumPitchPage extends StatelessWidget {
  const MaximumPitchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("MaximumPitchPage")));
  }
}
