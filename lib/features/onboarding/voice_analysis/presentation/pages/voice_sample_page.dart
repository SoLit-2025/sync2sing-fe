import 'package:flutter/material.dart';

class VoiceSamplePage extends StatelessWidget {
  const VoiceSamplePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("VocalSamplePage")));
  }
}
